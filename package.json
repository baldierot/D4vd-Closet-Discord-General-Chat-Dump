{
  "name": "d4vd-closet-discord-general-chat-dump",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "build:search-indexes": "node build-search-indexes.js",
    "build:search": "node build-search-indexes.js && node build-search.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "happy-dom": "^18.0.1",
    "vite": "^6.3.5",
    "vitest": "^3.2.1"
  }
}
